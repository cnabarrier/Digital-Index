# Enhanced Search — Predictive Dropdown, Section Filters & CAO Coverage

## Tasks

- [x] 1. Fix CAO search coverage — add CAO terms to haystack prefix, add `caos` to `sectionMeta`
- [x] 2. Build search index (`buildSearchIndex()`) — scans all `.cp` and `.leg-card` at page load
- [x] 3. Predictive autocomplete dropdown — HTML container, CSS styles, JS (showDropdown, hideDropdown, navigateToItem, keyboard nav)
- [x] 4. Section filter chips — filter bar below topbar, chip click filters search + dropdown
- [x] 5. Search highlights & section counts — `highlightText()`, `clearHighlights()`, section breakdown in multi-section results
- [x] 6. Update `tasks/todo.md` and `docs/architecture.md`
